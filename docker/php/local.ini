; Dev-stage PHP overrides (loaded as 20-local.ini; OPcache is in 10-opcache.ini)

[PHP]
upload_max_filesize = 100M
post_max_size = 100M
max_execution_time = 300
max_input_time = 300
memory_limit = 512M

[Date]
date.timezone = UTC

[Session]
session.gc_maxlifetime = 7200
